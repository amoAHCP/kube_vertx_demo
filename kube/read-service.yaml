apiVersion: v1
kind: Service
metadata:
  labels:
    name: read-verticle-dns
    visualize: "true"
  name: read-verticle-dns
spec:
  ports:
    - port: 5701
      name: hazelcast
  selector:
    app: vertx-cluster-01
