apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    name: write-verticle-dns
    visualize: "true"
  name: write-verticle-dns-controller
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: write-verticle-dns
        env: prod
        app: vertx-cluster-02
        visualize: "true"
    spec:
      containers:
      - image: gcr.io/kubernetestests-147607/write-verticle:02
        name: write-verticle-dns
        ports:
        - name: hazelcast
          containerPort: 5701
